{
  "name": "terrible-ghost",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "check": "node --check game.js && find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "typecheck": "tsc -p tsconfig.json",
    "release:check": "npm run check && npm run typecheck && node scripts/release-check.mjs",
    "electron": "electron electron/main.js"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "jsdom": "^29.1.1",
    "playwright": "^1.59.1"
  }
}
